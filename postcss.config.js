module.exports = {
    plugins: [
        require('tailwindcss'), // Use the Tailwind CSS plugin - allows to use tailwind utility classes and features
        require('autoprefixer'), // Use the Autoprefixer plugin - automatically adds vendor prefixes to CSS properties (ensures compatibility with different browsers)
    ]
}