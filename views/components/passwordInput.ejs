<!--password input where it can be required or not based on requirements of the page-->
<input type="password" id="<%= name %>" name="<%= name %>" <%= required ? 'required' : '' %> class="border-emerald-200 border-4 rounded-full py-1 px-3 mb-3">
